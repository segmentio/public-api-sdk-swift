//
// AudienceDefinitionEnhanced.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Enhanced audience definition that supports both AST and CQL formats. */
public struct AudienceDefinitionEnhanced: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case accounts = "ACCOUNTS"
        case users = "USERS"
    }
    /** The underlying data type being segmented for this audience.  Possible values: users, accounts. */
    public var type: ModelType?
    /** The target entity slug. */
    public var targetEntity: String?
    /** Array of conditions in different formats (AST, CQL). */
    public var conditions: [AudienceConditionsWrapper]?
    /** The query language string defining the audience segmentation criteria (CQL format). This is maintained for backward compatibility.  For guidance on using the query language, see the [Segment documentation site](https://segment.com/docs/api/public-api/query-language). */
    public var query: String

    public init(type: ModelType? = nil, targetEntity: String? = nil, conditions: [AudienceConditionsWrapper]? = nil, query: String) {
        self.type = type
        self.targetEntity = targetEntity
        self.conditions = conditions
        self.query = query
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case targetEntity
        case conditions
        case query
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(type, forKey: .type)
        try container.encodeIfPresent(targetEntity, forKey: .targetEntity)
        try container.encodeIfPresent(conditions, forKey: .conditions)
        try container.encode(query, forKey: .query)
    }
}

