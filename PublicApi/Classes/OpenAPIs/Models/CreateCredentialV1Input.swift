//
// CreateCredentialV1Input.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Create a new Credential based on a set of parameters. */
public struct CreateCredentialV1Input: Codable, JSONEncodable, Hashable {

    /** A human-readable name for this Credential. */
    public var name: String
    /** A key-value object that contains type-specific settings for this Credential.  `settings.type` is required and determines the Credential type (for example `Snowflake` or `Redshift`). Different types require different settings; see the `settings.type` discriminator in the OpenAPI spec for the full set of required and optional fields per type. */
    public var settings: JSON?

    public init(name: String, settings: JSON?) {
        self.name = name
        self.settings = settings
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case settings
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(settings, forKey: .settings)
    }
}

