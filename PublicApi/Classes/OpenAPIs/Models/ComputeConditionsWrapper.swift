//
// ComputeConditionsWrapper.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Query language definition wrapper. */
public struct ComputeConditionsWrapper: Codable, JSONEncodable, Hashable {

    public enum Format: String, Codable, CaseIterable {
        case ast = "AST"
        case cql = "CQL"
    }
    /** The query format. */
    public var format: Format
    /** The query language string or AST object defining the audience segmentation criteria. When format is 'AST', this should be an object. When format is 'CQL', this should be a string. Validation is handled at the business logic level due to conditional nature. */
    public var conditions: AnyCodable?

    public init(format: Format, conditions: AnyCodable?) {
        self.format = format
        self.conditions = conditions
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case format
        case conditions
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(format, forKey: .format)
        try container.encode(conditions, forKey: .conditions)
    }
}

