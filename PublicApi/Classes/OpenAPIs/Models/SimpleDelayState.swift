//
// SimpleDelayState.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct SimpleDelayState: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case simpleDelay = "SIMPLE_DELAY"
    }
    public var type: ModelType
    public var duration: String
    public var transitions: [Transitions]
    public var key: Key

    public init(type: ModelType, duration: String, transitions: [Transitions], key: Key) {
        self.type = type
        self.duration = duration
        self.transitions = transitions
        self.key = key
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case duration
        case transitions
        case key
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(type, forKey: .type)
        try container.encode(duration, forKey: .duration)
        try container.encode(transitions, forKey: .transitions)
        try container.encode(key, forKey: .key)
    }
}

