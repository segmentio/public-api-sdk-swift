//
// RuleInputV1.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Represents a rule to add to a Tracking Plan. */
public struct RuleInputV1: Codable, JSONEncodable, Hashable {

    public enum ModelType: String, Codable, CaseIterable {
        case common = "COMMON"
        case group = "GROUP"
        case identify = "IDENTIFY"
        case page = "PAGE"
        case screen = "SCREEN"
        case track = "TRACK"
    }
    /** The type for this Tracking Plan rule. */
    public var type: ModelType
    /** Key to this rule (free-form string like 'Button clicked'). */
    public var key: String?
    /** JSON Schema of this rule. */
    public var jsonSchema: AnyCodable?
    /** Version of this rule. */
    public var version: Double

    public init(type: ModelType, key: String? = nil, jsonSchema: AnyCodable?, version: Double) {
        self.type = type
        self.key = key
        self.jsonSchema = jsonSchema
        self.version = version
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case key
        case jsonSchema
        case version
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(type, forKey: .type)
        try container.encodeIfPresent(key, forKey: .key)
        try container.encode(jsonSchema, forKey: .jsonSchema)
        try container.encode(version, forKey: .version)
    }
}

